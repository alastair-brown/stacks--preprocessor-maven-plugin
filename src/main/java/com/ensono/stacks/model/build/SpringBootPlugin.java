package com.ensono.stacks.model.build;

class SpringBootPlugin extends ProjectPlugin {

    public SpringBootPlugin(String groupId, String artifactId, Object configuration) {
        super(groupId, artifactId, configuration);
    }
}
